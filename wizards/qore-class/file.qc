# -*- mode: qore; indent-tabs-mode: nil -*-

%new-style

@if '%{Base}'
class %{Class} inherits (%{Base})
@else
class %{Class}
@endif
{
    private {
    }

    public {
    }

    constructor() {
    }

} # class %{Class}

